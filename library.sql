create table books (
    id integer primary key,
    bookname    text,
    author      text,
    price       text,
    publisher   text,
    callnum     text,
    isbn        text,
    sortnum     text,
    pages       integer,
    pubdate     text,
    reno        integer
);
