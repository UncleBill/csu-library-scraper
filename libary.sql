create table books (
    id integer primary key,
    author      text,
    price       text,
    publisher   text,
    callnum     text,
    isbn        text,
    sortnum     text,
    pages       integer,
    pubdate     text
);
